name: Deploy to Server

on:
  workflow_run:
    workflows:
      - "Build & Push Frontend"
    types:
      - completed
    branches:
      - main

permissions:
  actions: read
  contents: read

concurrency:
  group: deploy-main
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    env:
      SERVER_HOST: ${{ secrets.SERVER_HOST || vars.SERVER_HOST }}
      SERVER_USER: ${{ secrets.SERVER_USER || vars.SERVER_USER }}
      SERVER_SSH_KEY: ${{ secrets.SERVER_SSH_KEY }}

    steps:
      - name: Wait for backend image build for same commit
        uses: actions/github-script@v7
        with:
          script: |
            const headSha = context.payload.workflow_run.head_sha;
            const sleep = (ms) => new Promise((resolve) => setTimeout(resolve, ms));

            for (let attempt = 1; attempt <= 40; attempt++) {
              const { data } = await github.rest.actions.listWorkflowRuns({
                owner: context.repo.owner,
                repo: context.repo.repo,
                workflow_id: "backend.yml",
                head_sha: headSha,
                per_page: 10,
              });

              const run = data.workflow_runs.find(
                (item) => item.name === "Build & Push Backend",
              );

              if (run?.conclusion === "success") {
                core.info(`Backend workflow succeeded for ${headSha}.`);
                return;
              }

              if (run?.status === "completed") {
                core.setFailed(
                  `Backend workflow finished with conclusion '${run.conclusion}' for ${headSha}.`,
                );
                return;
              }

              core.info(
                `Backend workflow for ${headSha} is not finished yet (attempt ${attempt}/40).`,
              );
              await sleep(15000);
            }

            core.setFailed(
              `Timed out waiting for backend workflow success for ${headSha}.`,
            );

      - name: Validate deploy secrets
        shell: bash
        run: |
          test -n "$SERVER_HOST" || { echo "Missing SERVER_HOST secret/variable"; exit 1; }
          test -n "$SERVER_USER" || { echo "Missing SERVER_USER secret/variable"; exit 1; }
          test -n "$SERVER_SSH_KEY" || { echo "Missing SERVER_SSH_KEY secret"; exit 1; }

      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ env.SERVER_HOST }}
          username: ${{ env.SERVER_USER }}
          key: ${{ env.SERVER_SSH_KEY }}
          script: |
            cd ~/projects/apps/ai-enterprise-intelligence
            docker compose -f docker-compose.prod.yml pull
            docker compose -f docker-compose.prod.yml up -d
            sleep 10
            docker compose -f docker-compose.prod.yml exec -T backend alembic upgrade head
            docker compose -f docker-compose.prod.yml exec -T backend python -m scripts.warm_preset_cache
